import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Your_Training_App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
